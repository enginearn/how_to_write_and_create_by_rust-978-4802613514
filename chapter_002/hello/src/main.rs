fn main() {
    let msg_list = [
        "A diligent person's plans always lead to success",
        "Impatient people always go to poverty."
    ];

    for msg in msg_list {
        println!("{}", msg);
    }
}
